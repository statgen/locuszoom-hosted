version: '3'

volumes:
  local_postgres_data: {}
  local_postgres_data_backups: {}
  local_rabbitmq_data: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: locuszoom_plotting_service_local_django
    depends_on:
      - postgres
    volumes:
      - .:/app
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - "8000:8000"
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: locuszoom_plotting_service_production_postgres
    ports:
      - "5432:5432"
    volumes:
      - local_postgres_data:/var/lib/postgresql/data
      - local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  redis:
    image: redis:3.2

  rabbitmq:
    image: rabbitmq:3.8
    volumes:
      - local_postgres_data:/var/lib/rabbitmq

  celeryworker:
    <<: *django
    image: locuszoom_plotting_service_local_celeryworker
    depends_on:
      - rabbitmq
      - postgres

    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: locuszoom_plotting_service_local_celerybeat
    depends_on:
      - rabbitmq
      - postgres

    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: locuszoom_plotting_service_local_flower
    ports:
      - "5555:5555"
    command: /start-flower
